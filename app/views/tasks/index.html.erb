<ul>
  <% @tasks.each do |task|  %>
    <li><%= "[]" if task.completed == false %>
        <%="[X]" if task.completed == true %>
      <%=link_to task.title, show_path(task.id) %></li>
<% end %>
</ul>
